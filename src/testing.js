// Función para sumar dos números
function sumar(a, b) {
    return a + c; // Error intencional: 'c' no está definido
}

// Llamada a la función
let resultado = sumar(5, 3);

console.log("El resultado es: " + resultado);